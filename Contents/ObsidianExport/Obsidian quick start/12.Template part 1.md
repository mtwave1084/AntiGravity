So now when we create new nodes, they are automatically ending up in the inbox and they are completely​‌新しいノードを作成すると、自動的に受信トレイに送られ、完全に

empty.​‌空の。

But maybe that's what you want.​‌しかし、おそらくそれがあなたが望んでいることなのでしょう。

But what I do is I every node that I create has an automatic template applied to it.​‌ただし、私が行っているのは、作成するすべてのノードに自動テンプレートを適用することです。

And I just wanted to quickly show you how I do that as well.​‌私もこれをどうやってやっているのか、簡単にお見せしたいと思いました。

It's again yet another plugin.​‌これもまた別のプラグインです。

That's why I like obsidian so much in line with Neovim that you can customize it so much with plugins.​‌だからこそ、私は Neovim と同様に、プラグインで非常にカスタマイズできる Obsidian がとても気に入っています。

So the plugin that we are going to use is Templater and I'm going to install this.​‌使用するプラグインは Templater なので、これをインストールします。

This is a template that's being used a lot.​‌よく使われるテンプレートです。

You can see it as 1 million downloads and it's a really, really good one.​‌100 万ダウンロードを達成したことは、本当に素晴らしいことだと言えます。

Let's just enable this by clicking the radio button covered by my camera.​‌カメラで覆われているラジオボタンをクリックしてこれを有効にしましょう。

But it is there.​‌しかし、それはそこにあります。

And now we have templater on And this actually unlocks a entire templating language in your vault.​‌そして、これで templater がオンになりました。これにより、実際に、vault 内のテンプレート言語全体がロック解除されます。

It's actually really cool.​‌実際、すごくかっこいいですよ。

But basically how it works is that you can add templates with hot keys, or you can, um, have templates​‌基本的には、ホットキーでテンプレートを追加したり、テンプレートを

added to, uh, to files that automatically.​‌自動的にファイルに追加されます。

So what I have in my vault is I have a in the top, top one.​‌私の金庫の中に入っているものは、一番上のものです。

I have a directory called templates here.​‌ここには templates というディレクトリがあります。

And in here I create a template or a note that is called uh Zettel template.​‌ここで、Zettel テンプレートと呼ばれるテンプレートまたはメモを作成します。

That's how I call it, because I use a zettelkasten, which I will get into in the further courses.​‌私はゼッテルカステンを使用しているので、このように呼んでいます。ゼッテルカステンについては、この後のコースで詳しく説明します。

but my my zettel template is it automatically has um, the it's almost empty but three lines down.​‌しかし、私の Zettel テンプレートでは、3 行下にはほぼ空の部分が自動的に表示されます。

It has a heading called links and then it has a line here.​‌「links」という見出しがあり、ここに行があります。

Let me just copy that over here.​‌それをここにコピーしておきます。

This is the syntax for the Templater plugin.​‌これは Templater プラグインの構文です。

You can go very deep on this.​‌これについては、非常に深く考えることができます。

You can do some pretty cool stuff.​‌かなりクールなものが作れます。

But what this does is that it adds a unique time stamp to each of my nodes, and that is part of the​‌しかし、これは各ノードに固有のタイムスタンプを追加するもので、これは

settle cast and methodology.​‌キャストと方法論を決定します。

But now we have set up this template.​‌しかし、今ではこのテンプレートを設定しました。

Then we can make sure that whenever a new node is created, um. ​‌そうすれば、新しいノードが作成されるたびに、