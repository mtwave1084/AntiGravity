So it's likely that you're going to be using your Obsidian vault on multiple systems.​‌したがって、Obsidian Vault を複数のシステムで使用することになる可能性が高くなります。

And I'm just going to show you how I've configured this to sync across different systems.​‌ここで、異なるシステム間で同期するようにこれをどのように構成したかを説明します。

Um, as I'm a macOS user and I'm using, uh, iPhone, I actually use iCloud sync for syncing across​‌えっと、私はmacOSユーザーで、iPhoneを使っているので、iCloud 同期を使って同期しています

devices.​‌デバイス。

So we didn't see this because I already have a synced, uh, vault when I started this vault, so.​‌つまり、この Vault を開始したときに、すでに同期された Vault があったため、これが表示されませんでした。

But when you are on macOS and you start up a new vault, then it will tell, ask you, do you want this​‌しかし、macOSで新しいVaultを起動すると、このVaultを希望するかどうか尋ねられます。

to be an iCloud vault?​‌iCloud の保管庫になるのでしょうか?

And then you can, uh, accept that option and then your iCloud will start sinking it.​‌そして、そのオプションを受け入れると、iCloud がそれを受信を開始します。

So I can, I can I can access this on my iPhone, I can access this on my iPad.​‌つまり、iPhone でも iPad でもこれにアクセスすることができます。

And I think that that's one of the biggest reasons why I still use, uh, Mac, because this, um,​‌これが私が今でもMacを使い続けている最大の理由の一つだと思います。

iCloud syncing works really well for obsidian.​‌iCloud 同期は Obsidian では非常にうまく機能します。

However, uh, I also respect that not everybody is, uh, able or willing to do this.​‌しかし、誰もがこれを実行できるわけではない、あるいは実行したいとも思っていないことも尊重します。

So, uh, what we can do is we can use a plugin called Obsidian Git to get, um, to sync our repository​‌そこで、Obsidian Gitというプラグインを使ってリポジトリを同期することができます

to GitHub.​‌GitHub へ。

And actually I do that too.​‌実際私もそうしています。

So if I go to my GitHub second brain here, this is my second brain in, uh, in git.​‌それで、ここで GitHub のセカンド ブレインにアクセスすると、これが Git 内の私のセカンド ブレインになります。

So yes, all of my thoughts, all of my deepest, uh, emotions and, and everything is stored in git.​‌そうです、私の考えや、私の心の奥底にある感情などすべてが Git に保存されているのです。

Uh, I'm selling my soul to Microsoft in a very literal sense.​‌ええと、文字通りマイクロソフトに魂を売っているんです。

Uh, they're starting git and GitHub, I should say, because for a long time I've been thinking of​‌ええと、彼らはgitとGitHubを始めていると言うべきでしょうか、私は長い間考えていました

just setting up a git server in my home lab.​‌自宅の研究室に Git サーバーをセットアップしています。

I just haven't gotten around to it.​‌ただ、まだその時間が取れていないだけです。

And my plan is to, uh, have my local git instance and then commit it there and then have an encrypted​‌私の計画は、ローカルのgitインスタンスを用意してそこにコミットし、暗号化された

one to GitHub, but But as of today, everything I write and own and my entire digital life and all​‌GitHubに1つありますが、今日現在、私が書いたものや所有しているもの、そして私のデジタルライフのすべて

of my thoughts are in GitHub and I, I, yeah, sell my soul to Microsoft.​‌私の考えは GitHub にあり、私は、ええ、Microsoft に魂を売り渡しています。

So that is one thing to consider.​‌それが考慮すべき点の1つです。

If you go to if you're going to back up to GitHub that, um, your stuff will be there.​‌GitHub にバックアップする場合は、あなたのデータはそこに保存されます。

And if you're concerned with someone accessing that, potentially even though it's a private repository,​‌たとえそれがプライベートなリポジトリであっても、誰かがそれにアクセスすることを心配しているのであれば、

then you might want to skip this step.​‌この手順をスキップしても構いません。

But it is a I do this because iCloud works well, but I want to have a some sort of second backup because​‌しかし、これはiCloudがうまく機能しているために行うのですが、何らかの二次バックアップが必要なので、

my entire life is, uh, put to port into here.​‌私の人生のすべてが、ここに集中しているんです。

Um, so with that being said, you can use Obsidian Git as a plugin.​‌ええと、そうは言っても、Obsidian Git をプラグインとして使用することができます。

But before we do that, that's why I had this open, because I had to, uh, refresh my memory.​‌しかし、それをする前に、記憶をリフレッシュする必要があったので、これを開いたのです。

Uh, we have to turn our vault into a git repository.​‌ええと、Vault を Git リポジトリに変換する必要があります。

So I'm in my git repository, and now I'm going to just, uh.​‌さて、私は Git リポジトリにいますが、これから、うーんと、やってみようと思います。

I'm sorry.​‌ごめんなさい。

I'm in my vault, the example vault that we were using.​‌私は自分の金庫、つまり私たちが使用していたサンプルの金庫の中にいます。

And now I can do git init, and now it's an empty git repository here.​‌そして、git init を実行できるようになりました。これで、ここに空の git リポジトリが作成されます。

And you can also see in my bash RC that it detected that there is some stuff happening here.​‌また、私の bash RC では、ここで何かが起こっていることが検出されたことも確認できます。

So what I'm going to do is in GitHub, I'm just going to go to my home page and repositories and then​‌GitHubでやることは、ホームページとリポジトリにアクセスして

create a new one.​‌新しいものを作成します。

And I'm just going to do this obsidian demo.​‌そして、私はこの Obsidian のデモを実行するつもりです。

And add myself as the owner.​‌そして、自分自身を所有者として追加します。

Let's create it.​‌作ってみましょう。

And then this is going to be the the URL of my git repo.​‌そして、これが私の git リポジトリの URL になります。

Um, yes.​‌うーん、はい。

This one we want.​‌これが欲しいんです。

Okay.​‌わかった。

So.​‌それで。

Oh, it actually says the entire sequence here as well.​‌ああ、実はここにもシーケンス全体が書いてあります。

I didn't, uh, realize that.​‌えーっと、気づきませんでした。

Okay, but we have this URL copied, and what I'm going to do is then I'm going to do git add dot.​‌さて、この URL をコピーしたので、git add dot を実行します。

So I'm adding all of the files git commit m initial commit.​‌そこで、git commit m の初期コミットにすべてのファイルを追加します。

Or I could just use lazy git for this, but I'm just going to do it the old good old fashioned way for​‌あるいは、lazy gitを使うこともできますが、ここでは昔ながらのやり方でやります。

now.​‌今。  今。‌今。

Then we're gonna do the git remote add.​‌次に、git remote add を実行します。  
次に、git Remote add を実行します。次に、git Remote add を実行します。

So we have that and we are going to do a push.​‌私たちはそれを持っており、プッシュするつもりです。

Okay.​‌わかった。

So so everything is, uh, pushed to the repository now.​‌つまり、すべてがリポジトリにプッシュされました。

Obsidian demo.​‌Obsidian のデモ。

And here we go.​‌さあ、始めましょう。

We have our, uh, initial commit.​‌最初のコミットが完了しました。

We have our nodes here.​‌ここにノードがあります。

We have all everything that we had here.​‌ここには持っていたものがすべて揃っています。

So that is, uh, all fine and dandy.​‌それで、ええと、すべて順調です。

So it's now backed up, and now we can go into obsidian, open up the settings, and then we're going​‌これでバックアップが完了しました。Obsidianを開いて設定を開き、

to add the.​‌を追加します。

Um, browse and then go add the is it git.​‌ええと、参照してから、git に追加します。

Yeah.​‌うん。

Back up your vault with git.​‌Git を使用して Vault をバックアップします。

Just this one install.​‌インストールはこれだけです。

So it is installed.​‌それでインストールされます。

Then we need to enable it.​‌次に、それを有効にする必要があります。

So you can do that from here or with the radio button that I showed you earlier.​‌そのため、ここから、または先ほど示したラジオ ボタンを使用してこれを行うことができます。

So now we have in community plugins we have git down here.​‌これで、コミュニティ プラグインに git がここにあります。

And here we have uh a certain settings.​‌ここで特定の設定を行います。

So uh commit and push changes after x minutes.​‌つまり、x 分後に変更をコミットしてプッシュします。

Um then we have to, um.​‌うーん、それなら、うーん。

Let's see.​‌見てみましょう。

How have I done this?​‌どうやってこれをやったのでしょうか?

Yes, yes, yes, I have this all standard.​‌はい、はい、はい、これはすべて標準です。

And I always do pull updates on startup.​‌そして、私は起動時に常にアップデートを実行します。

So everything is pulled when you start up, um, your obsidian vault, and this is all set up as standard,​‌Obsidian Vaultを起動すると、すべてが標準で設定されます。

I think.​‌私は思う。

Yeah, I think I haven't done much configure, I haven't done any other configuration.​‌そうですね、あまり設定をしていないと思います。他の設定もしていません。

And this actually.​‌そして実はこれ。

But what I do is um, let me see.​‌でも、私が何をするかって、えーと、ちょっと考えさせてください。

I have this set to pull every 5 or 10 minutes or so because Because if I'm on different systems, then​‌私はこれを5 分か10 分おきに実行するように設定しています。なぜなら、異なるシステムを使用している場合、

I want it to pull and let's see.​‌引っ張って見てみようと思います。

Um.​‌えーっと。

I have this.​‌これを持っています。

Let me just check my vault here.​‌ここで私の金庫を確認させてください。

I have this set to 15 minutes.​‌私はこれを15 分に設定しました。

So every 15 minutes it's going to check.​‌つまり 15 分ごとにチェックすることになります。

And if there is a backup or if there are changes.​‌バックアップがあるか、変更があるかなど。

And then I have this set to on like this and this.​‌そして、これをこのように設定しました。

I have set to ten okay.​‌10に設定しておきました。

So now if I add a couple of notes.​‌さてここでいくつかメモを追加します。

So let's do command new hello world.​‌それでは、コマンド new hello world を実行してみましょう。

123 and git test 123.​‌123 と git test 123。  
123 と git test 123.‌123 と git test 123。

Okay so now we have a bunch of nodes in here.​‌さて、ここにはたくさんのノードがあります。

And now if I do uh command p.​‌さて、ここでコマンドpを実行します。

So this is all of the commands you can do in obsidian.​‌これが Obsidian で実行できるすべてのコマンドです。

And if I then press get then we have all of these git commands here.​‌そして、「get」を押すと、これらすべての git コマンドがここに表示されます。

But you can also do backup git create backup.​‌ただし、git create backup でバックアップを実行することもできます。

So this is a way you can test if everything is working correctly.​‌したがって、これはすべてが正しく動作しているかどうかをテストできる方法です。

So I'm just going to press enter here.​‌ではここで Enter キーを押します。

So it has committed and it has pushed eight files to the remote.​‌つまり、コミットされ、8 つのファイルがリモートにプッシュされました。

And if I go to my remote, refresh it.​‌リモコンに移動したら、更新します。

We see here we have a vault backup.​‌ここに、Vault のバックアップがあることがわかります。

And just just now and it created this commit.​‌そしてたった今、このコミットを作成しました。

And in here you see that it also commits the obsidian settings like this.​‌ここでは、Obsidian の設定もこのようにコミットしていることがわかります。

But if we go to our inbox we have our git test node and our inbox hello world node.​‌しかし、受信トレイに移動すると、git テスト ノードと受信トレイの hello world ノードがあります。

So very easily, very quickly we have set our vault to be backed up to git if that is what we would​‌非常に簡単に、非常に素早く、私たちは、それが私たちが望むことであれば、VaultをGitにバックアップするように設定しました。

prefer.​‌好む。

So this is a really good plugin.​‌これは本当に優れたプラグインです。

Like I said, I use both iCloud, um, I use iCloud for syncing across my devices, but then I use Git​‌先ほど言ったように、私はiCloudとGitの両方を使っています。デバイス間の同期にはiCloudを使っていますが、

or Obsidian git to backup my vault at regular intervals. ​‌または、Obsidian git を使用して、定期的に vault をバックアップします。