[[19.実演用コマンド]]

それでは、WINDOWSのパソコンでクローズコードを動かすために必要な手順について解説をしていきます。

まず、WINDOWSでクローズコードを動かすためには必要な手順がありますので、順を追ってお話をしていきます。

1つ目がWINDOWSのこのボタンありますよね。

こちらのボタンに対して右クリックしてください。

そうすると、ターミナルと出て来ます。

このターミナルを押して下さい。

するとWINDOWSPOWERSHELLという表記が出てくるかとおもいます。

このWINDOWSPOWERSHELLが開いたら、こちらの入力欄からこれからコマンドを入力をしていきながら進めていきたいと思います。

まず、クロードコードをインストールするためには。

NODEJSというプログラムが必要になってきます。

なので、皆さんのパソコンの中にこのプログラムがインストールされているかどうかというのを確認していこうと思います。

では。

ノードスペースハイフンv。

こちらのコマンドを入力してENTERを押しましょう。

このコマンドは本動画のレクチャー内にコピーアンドペーストができるように貼ってありますので、こちらをコピーして貼り付けをしてください。

入力が完了したらENTER。

ENTERを押すと2パターンの表記に分かれるかと思います。

まず1つ目が、今私の画面が映っているような形で、V224171のような形でバージョンが表記されるパターンですね。

このようにバージョンが表示されていたら、既にインストールが完了している状態です。

このバージョンの数値が18以上の数字が表示されていましたら、次のステップに進んでいってください。

一方で、このコマンドを入力した後にエラーのような表記インストールがされていません。

といった赤い文字で表記されているケース。

こちらに関しては、まだ皆さんのパソコンの中に必要なプログラムがインストールされていない状態ですので、いっしょにインストールを進めていきましょ

う。

ではですね、このレクチャーの中にNODEJSのURLを貼ってありますので、こちらを開いてください。

開いていただきますと、下のほうにNODEJSを入手とありますので、こちらのボタンをタップ。

画面上部のほうにNODEJSをダウンロードするという表記の下にWINDOWS用のNODEJSのこちらですね、バージョンが新しい表記のものでNTS

といったものを選んでください。

このバージョンの数値というのは、皆さんのダウンロードするタイミングで数値が変わるかと思います。

で、この中のFPMのDOCKERというふうになっています。

左下ですね。

WINDOWSインストーラーをクリックしてインストールを完了させてください。

では、先程開いたターミナルですね。

こちらは一旦バツ印で閉じてしまって下さい。

閉じていただいて、同じようにWINDOWSのこちらのボタンから右クリックをして、ターミナルを開いて頂いたら、先ほどと同じコマンドを入れ

ていきましょう。

はい、ノードスペースハイフンv。

こちらをENTERを押していただけると。

私と同じような形でバージョン表記がされてインストールされているよと出ると思います。

もしこちらのターミナルを閉じないでそのまま入力をしてしまうと、場合によってはですね、NODEJSがインストールされているにも関わらず、

インストールされていませんと表記が出るケースがありますので、一旦ですね、こちらのターミナルを閉じてから再起動するのをお勧めします。

まずここまででNODEJSをインストールしていきました。

続いてGITのインストールをしていきたいとおもいます。

まず、GITというツールが入っているかどうかをチェックするために、同じようにこちらのコマンドを入力してみてください。

GITSPACEハイフンルートです。

このコマンドに関しても本動画のレクチャーのリソース欄に記載がございますので、こちらをコピーアンドペーストして貼り付けしてください。

貼り付けが終わったらENTERENTERを押しますと、私の画面のようにGITVERSION00と出ていると、このパソコンにGITというツール

が入っているよという表記になります。

一方で、先ほどのNODEJSと同じように赤い文字でエラー表記が出る場合に関しては、まだGITが皆さんのパソコンの中にインストールがさ

れていませんので、こちらをインストールしていきましょう。

リソース欄にGITのURLが貼ってありますので、こちらにアクセスをしてください。

アクセスをしていただいたら、ホーム画面の中央部分にダウンロードボタンがあります。

こちらを押していただいて。

ダウンロードを完了してください。

ダウンロードが完了しますと。

インストーラーが起動出来ますので。

インストーラーを起動していただいて。

GITのインストールを進めていってください。

インストールをする際にはですね。

いろいろ設定する項目があるんですけども。

基本的にはそのままの設定でNEXTボタンを押して頂いてインストールを完了させていきましょう。

ではまたターミナル画面に戻って頂いて。

ターミナルも一度閉じて。

WINDOWSボタンで右クリックを押してターミナルを選択。

この画面に戻って頂いたら、先ほどのコマンドGITハイフン先ほどのコマンドを入力して下さい。

そうするとGITVERSION00といった表記が出てくるかと思います。

ではですね、今回はクラウドコードをインストールするための準備編という形で、NODEJSとGITのインストールをターミナルを使って行って

きました。

分からないポイントがありましたら、動画を何回か止めて作業を進めていってください。

よろしくお願いします。